/**
 * DailyDrillBlocker Configuration Constants
 */

// Exempt Routes Configuration - routes that don't require daily drill
export const EXEMPT_ROUTES = ['/login', '/register', '/dashboard', '/settings', '/analytics', '/aar', '/failure-log'];
