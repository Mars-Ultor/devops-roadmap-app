# ML Service Environment Variables Template
# Copy this to .env and fill in your values

# Database Configuration (same as server)
# Development: SQLite (file-based, easy setup)
# Production: PostgreSQL (scalable, robust)
#
# For Development (SQLite - relative path to server):
# DATABASE_URL="file:../server/prisma/dev.db"
#
# For Production (PostgreSQL):
DATABASE_URL="postgresql://username:password@host:5432/database"

# Server Configuration
PORT=8000
HOST="0.0.0.0"
WORKERS=2

# Environment
NODE_ENV="production"

# Optional: External Services
# Redis for caching (improves performance for ML predictions)
REDIS_URL="redis://localhost:6379"
# SENTRY_DSN="https://..."
# LOG_LEVEL="INFO"