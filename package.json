{
  "name": "devops-roadmap-integration-tests",
  "version": "1.0.0",
  "description": "Integration tests for DevOps Roadmap App",
  "main": "integration-test.js",
  "scripts": {
    "test:integration": "node integration-test.js",
    "test:load": "k6 run load-test.js",
    "test:load:ci": "k6 run --out json=load-test-results.json load-test.js",
    "test:all": "npm run test:integration && npm run test:load"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "chai": "^4.3.10"
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}